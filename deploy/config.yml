# deploy/config.yml

# branchs:
  # test
  # production
  # canary

repositories: syssoftintegra/catalog

clients:

  sysintegra:
    service: sysintegra-services
    deploy: test

  leatsac:
    service: leatsac-services
    deploy: canary

  importmuneli:
    service: importmuneli-services
    deploy: canary